

export const metadata = {
    title: " Admin Panel ",
    description: " controle all app ",
  };

export default function RootLayout({ children }) {
    return (
      <div >
        {children}
      </div>
    );
  }